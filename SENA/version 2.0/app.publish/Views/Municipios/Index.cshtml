@model IEnumerable<Proyecto.Models.Municipio>

@{
    ViewBag.Title = "Index";
}

<h2>Municipios</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create",new { }, new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            <h3>@Html.DisplayNameFor(model => model.Nombre)</h3>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            <h4>@Html.DisplayFor(modelItem => item.Nombre)</h4>
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.MunicipioId }, new { @class = "btn btn-primary" }) 
            @Html.ActionLink("Detalles", "Details", new { id=item.MunicipioId }, new { @class = "btn btn-success" }) 
            @Html.ActionLink("Eliminar", "Delete", new { id=item.MunicipioId }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
